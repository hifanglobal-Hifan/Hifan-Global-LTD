import PricingDeliveryContent from '@/components/pages/PricingDeliveryContent';

export const metadata = {
    title: 'Pricing & Service Delivery | Hifan Global',
    description: 'Learn how Hifan Global LTD prices its services, the currencies we charge in, and what to expect in terms of deliverables and timeframes.',
};

export default function PricingServiceDeliveryPage() {
    return <PricingDeliveryContent />;
}
