import BlogContent from '@/components/pages/BlogContent';

export const metadata = {
    title: "Blog | Hifan Global",
    description: "Stay updated with the latest insights, news, and guides on IT, Marketing, and Consultancy from Hifan Global.",
};

export default function BlogPage() {
    return <BlogContent />;
}
